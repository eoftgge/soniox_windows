language_hints = ["ru", "en"]
context = "context for Soniox"
api_key = "..."
level = "debug"
target_language = "ru"
enable_translate = false
enable_speakers = false  # it's don't work at current moment
